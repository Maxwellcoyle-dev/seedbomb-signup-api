{
  "name": "seedbomb-signup-api",
  "version": "1.0.0",
  "description": "Signup microservice for SeedBomb CRM",
  "type": "module",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest --watch"
  },
  "author": "Max Henderson",
  "devDependencies": {
    "vitest": "^3.1.3"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.810.0",
    "@aws-sdk/client-eventbridge": "^3.810.0"
  }
}
